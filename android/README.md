# Android

```bash
npm install
npx cap add android
npx cap sync android
cd android && ./gradlew assembleDebug
```

APK: `app/build/outputs/apk/debug/app-debug.apk`

Данные с веба: Ещё → Выгрузить / Загрузить данные.
