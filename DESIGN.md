# Дизайн Стенографа для Windows / Flet

Эталон — превью песочницы Grok (`src/styles.css`, `src/components/stenograf/`).
ПК-версия должна **выглядеть как эта студия**, а не как дефолтный Material/Flet.

Не копировать React. Копировать **палитру, тип, ритм, иерархию экранов**.

---

## 1. Характер

Тихий тёмный инструмент, не дашборд и не «нейросеть будущего».

- Почти чёрный камень, не синий `#0f172a` и не фиолетовый Flet.
- Заголовки — антиква (как Fraunces). Текст — нейтральный гротеск.
- Акцент записи — тёплая терракота, не ядовитый red-600.
- Мало хрома: одна главная кнопка на студии, остальное вторично.
- Скругления спокойные (8–16), не stadium-пилюли на всём.

Если экран выглядит как админка или как ChatGPT — мимо.

---

## 2. Цвета (hex из src/styles.css)

| Токен | Hex | Куда |
|---|---|---|
| bg | `#0B0C0E` | фон окна |
| surface | `#141518` | карточки, шапка |
| surface-2 | `#1C1D21` | тихие кнопки, полоски |
| surface-3 | `#26272C` | hover |
| fg | `#ECECE8` | текст |
| muted | `#9A9B97` | подписи |
| subtle | `#6E6F6C` | вторичное |
| border | `#2A2B2F` | линии |
| primary | `#ECECE8` | светлая кнопка |
| primary-fg | `#0B0C0E` | текст на ней |
| rec | `#C45C4A` | запись, точка |
| rec-fg | `#FFF8F6` | текст на записи |
| ok | `#7D9A78` | успех, ручная связь |
| warn | `#C4A574` | согласие |
| ring | `#C8C4BC` | фокус |

Запрещено: `#6750A4`, `#2563EB`, неон, чистый `#FF0000`.

```python
page.bgcolor = "#0B0C0E"
page.theme_mode = ft.ThemeMode.DARK
page.theme = ft.Theme(
    color_scheme=ft.ColorScheme(
        primary="#ECECE8",
        on_primary="#0B0C0E",
        surface="#141518",
        on_surface="#ECECE8",
        outline="#2A2B2F",
        error="#C45C4A",
    )
)
```

Карточка: `bgcolor="#141518"`, `border=ft.border.all(1, "#2A2B2F")`, `border_radius=16`.

---

## 3. Шрифты

| Роль | Семья | Размер |
|---|---|---|
| Витрина | Fraunces / Georgia / Constantia | 28–36, tracking сжатый |
| Текст | Source Sans 3 / Segoe UI | 14–16, line-height ~1.45 |
| Моно | IBM Plex Mono | шаблоны |

Шапка: «Стенограф» + muted «Диктофон · карта · напоминания».
Не Caps Lock, не логотип-мозг. Шрифты в `assets/fonts/`, в exe без Google Fonts.

---

## 4. Сетка

Контент ≤ ~1150 px, поля 16–24.
Desktop ≥1100: контент + колонка задач 300 px.
Низ: 7 вкладок, иконка + подпись 10–11 px.
Шапка: квадрат 36×36 surface-2, поиск, точка записи.
Кнопки 48 / 36–40. Радиусы: 12 кнопка, 16 карточка, 24 мастер, 999 чип.

---

## 5. Экраны

**Студия.** Встреча → Слушать / Стоп / Непрерывно / Протокол → RMS + waveform → уточнение → лента. Железо не здесь.
**Карта.** Полотно bg, узлы surface-2, выбранный/новая связь ok. + − сброс — мелкий outline.
**Задачи.** Карточки, не DataTable. Справа на широком окне.
**Сроки.** Список + месяц, не Outlook.
**Протокол.** Документ, не таблица.
**Ещё.** Группы: устройство, модель, JSON, обновление.
**Мастер.** Один шаг — одна мысль, кнопка на всю ширину.
**Compact.** Плашка: точка rec, «слушаю», RMS, Протокол, Развернуть.

---

## 6. Кнопки

| Вид | Фон | Текст |
|---|---|---|
| Главная | `#ECECE8` | `#0B0C0E` |
| Запись | `#C45C4A` | `#FFF8F6` |
| Outline | прозрачный + border | fg |
| Ghost | прозрачный | fg, hover surface-2 |

Запись: точка 8 px `#C45C4A` + «Идёт запись» в шапке.
Непрерывный режим не прятать в настройки. Фокус `#C8C4BC`.

---

## 7. Движение

150–250 мс, `cubic-bezier(0.23, 1, 0.32, 1)`.
Новое ребро карты ~0.55 с. Waveform ≤ 12 fps.

---

## 8. Не делать

Фиолетовый Flet, иконки 32 px на всех вкладках, прогресс модели на студии, светлая тема в v1, тяжёлые тени, «12 графиков».

---

## 9. Приёмка

Окно Flet рядом с превью Grok: те же hex, антиква в титуле, одна большая кнопка слушать, терракота записи, 14" 150% без обрезки.
Иначе UI не готов, даже если STT работает.
