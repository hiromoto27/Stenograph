name: Test
on:
  push:
    branches: ["main"]
  pull_request:
jobs:
  unit:
    name: Unit tests (worker logic)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install numpy
      - run: python -m unittest discover -s worker -p "test_*.py" -v
