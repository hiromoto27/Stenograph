# Логика Стенографа (эталон песочницы)

Для агентов neural-dictaphone / Flet. Поведение важнее синтаксиса TS. Пороги не менять без причины.

Эталон: `src/lib/store.ts`, `classifier.ts`, `intent.ts`, `utterance-meta.ts`, `essence.ts`, `graph.ts`, `archive-search.ts`.

## 1. Поток фразы

текст → parseSpeechIntent → если createTask: addTask + feedback+ → иначе rankTasks + neuralBoost → ambiguous? уточнение → иначе авто если score ≥ 0.52 → remindAt на задачу.

Пусто / <2 символов — выбросить. Реплика всегда в ленту (до 400), даже без задачи.

## 2. Интенты

Жёстко: сделай|создай|поставь|заведи|добавь|запиши|открой + задач[уие].
Мягко: предложение с необходимо|нужно|надо.
Заголовок: вырезать маркеры, капитализировать.
Напоминание отдельно: завтра в 10, через 2 часа, в 18:00 → Date, reminded=false.
Интент раньше классификатора.

## 3. Токены

lower, ё→е, стем одного окончания если длина≥5: ами|ями|ого|ему|ыми|ими|ов|ев|ей|ах|ях|ам|ям|ом|ем|ой|ый|ий|ая|ое|ые|ие|ть|ти|ла|ли|ло|ка|ки.
Стоп-слова и длина<3 вон. Не Porter, не pymorphy. Мешок + cosine.

## 4. Ранг задачи

score = clamp(cosine(query, positive∪title) - 0.45*cosine(query, negative) + min(0.22, 0.05*|overlap|), 0, 1)
final = min(1, score*0.7 + cosine(embed text, embed title+notes+positive)*0.3)
Порог авто 0.52. Топ-3 в карточку. В Flet эмбеддинг = Chroma, мешок слов не удалять.

## 5. Уточнение, не авто

linkedAmbiguous: есть 1 и 2 место, у обоих overlap, разница скоров ≤0.16, и связь links или общие слова или разница<0.1.
Причина: связи / ключевые слова / «нужно подтверждение».

## 6. Обучение

Авто: positive += 1.25 на токен, hits+1.
Уточнение: выбранной +; другим кандидатам −; suggested верно hits, неверно misses; links между выбранной и соседями +1.
Уровень: hits/(hits+misses), <3 Новичок, ≥85 Эксперт, ≥70 Уверенный, ≥50 Учится.

## 7. Kind

Решение: утвердили 0.9, приняли решение 0.9, постановили 0.85, договорились 0.85, решили 0.8…
Риск / блокер — свои словари.
«Не решили» / «надо утвердить» → score решения ×0.15.
Порог 0.6, иначе speech. Роли me/other — руками.

## 8. Встречи и паузы

meeting_id на репликах. end → пакет или протокол.
STT-пауза ~1 с ≠ pause_sec протокола (8 с). Кнопка «Протокол» всегда.

## 9. Протоколы

Плейсхолдеры {{title}} {{date}} {{tasks}} {{decisions}} {{risks}} {{unassigned}} {{actions}}.
Пакет: протокол + действия + выжимка + черновик гайда. До 50 шт. Удаление строк ленты обязательно.

## 10. Карта

Хаб в центре, задачи по кругу. Авторёбра по сходству + map_links − hidden_pairs.
На узле taskEssence (без «короче/типа»), не простыня.
Тащить узел на узел (~58 px) → linkTasks. Клик по ребру → hidePair. Анимация 0.55 с.

## 11. Напоминания

remind_at, repeat_min, due_at, reminded. Тик пока жив процесс. Отложить = +минуты, reminded=false. Экспорт .ics.

## 12. Поиск

Те же tokenize. Доля совпавших стемов. Задача +0.15. Топ 20.

## 13. STT

Один worker, очередь на диск, language=ru, не звать на тишине, фильтр галлюцинаций, 16 kHz, без condition_on_previous_text в continuous. Модель вне exe, sha, не перекачивать.

## 14. Данные

Снимок без recording/clarify. JSON экспорт/импорт. 400 реплик, 50 протоколов.

## 15. Порядок в Flet

1 ingest+intent+ambiguous+feedback
2 kind+шаблоны+удаление строк
3 встречи и две паузы
4 карта essence+drag-link
5 напоминания+ics
6 поиск

Классификатор без уточнений — не Стенограф.
